import java.util.*
import kotlin.Comparator

fun main() {
	val subtract = mutableListOf<String>()
	val a = List(readLine()!!.toInt()) { readLine()!! }

	var res = ""

	a.forEachIndexed { index, s ->
		s.forEach {
			if(a[0])
		}
	}








}
// abcd1
// bcdd1
// as1w

// a